<%= form_for(report_category) do |f| %>
  <% if report_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(report_category.errors.count, "error") %> prohibited this report_category from being saved:</h2>

      <ul>
      <% report_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
